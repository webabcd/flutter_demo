/*
 *
 */

import 'package:flutter/material.dart';

class ScaffoldPersistentFooterButtonsDemo extends StatelessWidget {
  const ScaffoldPersistentFooterButtonsDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        width: 50,
        height: 50,
        color: Colors.red,
      ),
    );
  }
}